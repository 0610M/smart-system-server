<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.smartAnswerAssContent.mapper.SmartAnswerAssScoreMapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE 
		FROM  smart_answer_ass_score 
		WHERE
			 main_id = #{mainId} 
	</delete>
	
	<select id="selectByMainId" parameterType="java.lang.String" resultType="org.jeecg.modules.smartAnswerAssContent.entity.SmartAnswerAssScore">
		SELECT * 
		FROM  smart_answer_ass_score
		WHERE
			 main_id = #{mainId} 
	</select>
	<select id="listAllByAssContentIdAndMainIdAndRoleId"
			resultType="org.jeecg.modules.smartAnswerAssContent.entity.SmartAnswerAssScore">
		SELECT
		saac.id, saac.main_id, score
		FROM
		smart_answer_ass_content saac
		LEFT JOIN smart_answer_ass_score saas ON saas.main_id = saac.id
		WHERE saac.main_id in
		<foreach collection="mainIds" item="mainId" open="(" separator="," close=")">
			#{mainId}
		</foreach>
		and ass_content_id = #{assContentId}
		and role_id = #{roleId}
	</select>
</mapper>
